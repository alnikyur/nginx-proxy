# Nginx load balancing example configuration

### This is an example how to configure nginx load balancing in docker container

To use just run `docker-compose up` command.

HTTP port: 8080
HTTPS port: 8443
